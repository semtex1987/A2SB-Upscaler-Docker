"""Backend for the A2SB restoration UI.

The modules here are deliberately free of any web-framework imports below
`api`/`main`, so the restoration pipeline stays callable from scripts and tests.
"""
