## 2024-05-18 - Contextualize Helper Text
**Learning:** Users benefit more from contextual helper text attached directly to UI components rather than large instruction blocks at the top of the page.
**Action:** Use Gradio's `info` attribute on components, or a localized `gr.Markdown` with utility classes, to provide inline guidance.
