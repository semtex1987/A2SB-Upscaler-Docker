## 2026-07-02 - Localized Helper Text
**Learning:** Moving global instruction blocks into component-specific `info` attributes or localized markdown blocks (for components without `info` support) drastically improves form usability by providing context exactly where the user needs it.
**Action:** Prefer localized helper text over large instructional blocks at the top of forms.
