## 2024-05-01 - Avoid wall-of-text explanations
**Learning:** Avoid wall-of-text explanations. Use the `info` parameter for supported components (e.g., Textbox, Slider) to improve discoverability.
**Action:** When designing Gradio interfaces, prefer component-level `info` parameters over large, unassociated `gr.Markdown` blocks.
