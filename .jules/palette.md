## 2024-06-24 - Move global instructions to context-specific info
**Learning:** Moving large, global instructional text blocks into context-specific info properties on the relevant inputs significantly reduces initial cognitive load and improves user focus.
**Action:** Always prefer using the `info` attribute on inputs for contextual help instead of global `gr.Markdown` blocks.
