## 2026-06-03 - Contextual UI Helpers
**Learning:** Moving global instruction blocks into contextual `info` attributes on Gradio components reduces cognitive load and improves focus.
**Action:** Prefer localized `info` attributes or small helper text over large blocks of text at the top of the app.
